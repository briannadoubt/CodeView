# CodeViewDemo iOS

This folder is reserved for a companion iOS/iPadOS demo app. The package already includes `CodeViewDemoSupport`, which exposes sample files and preview-ready SwiftUI content for a future standalone app target.
